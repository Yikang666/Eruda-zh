import Tool from '../DevTools/Tool.es6'

export default class Elements extends Tool
{
    constructor()
    {
        super();
        this.name = 'elements';
    }
    init($el)
    {
        super.init($el);
    }
}