import Tool from '../DevTools/Tool.es6'

export default class Settings extends Tool
{
    constructor()
    {
        super();
        this.name = 'settings';
    }
    init($el)
    {
        super.init($el);
    }
}